{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "gffd",
    "title": "genome-first-functional-profiling pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "minLength": 3
            },
            "fastq1": {
                "type": "string",
                "pattern": "^\\S+\\.f.+[aq](\\.gz)?$",
                "errorMessage": "FASTA/FASTQ file for reads must be provided, cannot contain spaces and must have extension '.fq', '.fastq', '.fa' or '.fasta' with optional '.gz' afterwards."
            },
            "fastq2": {
                "errorMessage": "FastQ file for reads 2 cannot contain spaces and must have extension '.fq.gz' or '.fastq.gz'",
                "anyOf": [
                    {
                        "type": "string",
                        "pattern": "^\\S+\\.f(ast)?q\\.gz$"
                    },
                    {
                        "type": "string",
                        "maxLength": 0
                    }
                ]
            },
            "single_end": {
                "type": "boolean",
                "errorMessage": "Runs must be labelled as single-end (true) or paired-end (false)"
            }
        },
        "required": ["sample", "fastq1", "single_end"]
    }
}
